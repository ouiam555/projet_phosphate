name: 'ocp_analytics'

version: '1.0.0'

config-version: 2

profile: 'ocp_analytics'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  ocp_analytics:
    staging:
      +materialized: view
      +schema: BRONZE

    silver:
      +materialized: table
      +schema: SILVER

    gold:
      +materialized: table
      +schema: GOLD